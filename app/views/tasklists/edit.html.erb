<h4>Rename Task List</h4>
<%= form_for @tasklist  do |f| %>
  <div class="form-new-list">
    <%= f.label(:title, "Title") %>
    <%= f.text_field :title %>

    <%= f.submit "Submit", class: "btn btn-success" %>
  </div>
<% end %>
